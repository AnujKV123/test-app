name: Deployment

on:
    workflow_dispatch:

jobs:
  linting:
    name: Lint Ansible files
    runs-on: ubuntu-latest

    steps:
    - name: Checkout sources
      uses: action/checkout@v2
    - name: Linting
      uses: ansible/ansible-lint-action@c0893adbf8d7d1cf2265e0aaf847f3d4d1e4f3c1